<form action="{{ path('manage_delete', {'type': 'tag', 'id': tag.id}) }}" method="post">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tag.id) }}">
    <button class="btn">Delete</button>
</form>